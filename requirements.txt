# Neural-Mosaic — runtime dependencies (CPU-only; no GPU required)
#
# Both mosaic engines (Smart Photo + Symbol/Typo) run entirely on CPU.
# Tested on Python 3.10 (conda env `mosaic`).
#
#   pip install -r requirements.txt
#
# This is a curated list of the packages the application actually imports.
# (pip will resolve their transitive dependencies automatically.)

# --- GUI ---
customtkinter==5.2.2
matplotlib==3.10.9          # LAB Coverage Map plot embedded in the GUI

# --- Imaging / mosaic core ---
pillow==11.1.0
numpy==2.0.1
scipy==1.15.3               # cKDTree tile matching
scikit-image==0.25.2
scikit-learn==1.7.2
imagehash==4.3.1            # near-duplicate tile filtering

# --- Typography engine ---
fonttools==4.63.0           # glyph cmap inspection (no .notdef tofu boxes)

# --- Tile downloaders (async) ---
aiohttp==3.13.2
aiofiles==25.1.0
requests==2.32.5

# --- Misc ---
tqdm==4.67.1
python-dotenv==1.2.1

# --- Optional: dormant MiDaS depth module (src/ai_core.py) -------------------
# Not used by the runtime mosaic engines. Install only if you enable the
# legacy depth-estimation path (large download, ~GB):
#   torch==2.5.1
#   torchvision==0.20.1
#   transformers==4.57.3
